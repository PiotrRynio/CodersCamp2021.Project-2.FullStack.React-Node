import React from 'react';
import { Container, Nav } from './Navigation.styled';

const Navigation = () => {
  return (
    <Container>
      <Nav>Navigation</Nav>
    </Container>
  );
};

export default Navigation;
