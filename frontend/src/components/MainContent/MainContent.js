import React from 'react';
import { MainContainer, Main } from './MainContent.styled';

const MainContent = () => {
  return (
    <MainContainer>
      <Main>Main</Main>
    </MainContainer>
  );
};

export default MainContent;
